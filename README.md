# Notes API (FastAPI, async)

## Быстрый старт (локально)
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload

## Эндпоинты
- /auth
- /notes

Swagger: /docs
